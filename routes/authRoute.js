// const express = require("express");
// const router = express.Router();
// const { signup, signin, forgotPassword, resetPassword } = require("../controllers/authController");
// const { controllerLimiter } = require("../middleware/rateLimiter");

// router.post("/signup", controllerLimiter, signup);
// router.post("/signin", controllerLimiter, signin);
// router.post("/forgot-password", controllerLimiter, forgotPassword);
// router.post("/reset-password/:token", controllerLimiter, resetPassword);

// module.exports = router;
